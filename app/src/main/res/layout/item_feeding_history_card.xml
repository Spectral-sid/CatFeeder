<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Имя питомца (опционально) -->
        <TextView
            android:id="@+id/tv_history_pet_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="?attr/colorPrimary"
            android:layout_marginBottom="4dp"/>

        <!-- Верхняя строка с датой и временем -->
        <TextView
            android:id="@+id/tv_history_datetime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="?attr/customTextSecondary"
            android:layout_marginBottom="8dp"/>
        <!-- НОВЫЙ ЭЛЕМЕНТ: статус съеденного -->
        <TextView
            android:id="@+id/tv_was_finished"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:background="@drawable/bg_status_badge"
            android:visibility="gone"/>

        <!-- Название корма -->
        <TextView
            android:id="@+id/tv_history_food_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?attr/customTextPrimary"
            android:layout_marginBottom="4dp"/>

        <!-- Нижняя строка с количеством -->
        <TextView
            android:id="@+id/tv_history_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

        <!-- Заметки -->
        <TextView
            android:id="@+id/tv_history_notes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="?attr/customTextSecondary"
            android:visibility="gone"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>